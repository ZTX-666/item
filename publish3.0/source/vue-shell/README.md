# Vue 对接示例

最小 Vue 3 前端，演示如何调用 `WacliDesktop.ApiHost`。

## 使用

1. 在项目根目录运行 `start-api.bat`
2. 本目录执行：

```bash
npm install
npm run dev
```

3. 打开 http://127.0.0.1:5173

## 集成到你自己的 Vue 系统

复制 `src/api/wacli.ts` 到你的项目，或按 `../docs/VUE_API_INTEGRATION.md` 自行封装。
