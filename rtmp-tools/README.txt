3311 AI - Quick start
=====================

Conversation summary (Chinese): SESSION_NOTES.md

RTMP snapshot script:
  pip install -r requirements-rtmp-snapshot.txt
  python rtmp_snapshot.py --once

Snapshots save to current directory unless you pass -o "path".

Default stream URL is set in rtmp_snapshot.py (DEFAULT_RTMP).
